# docker_emulator_pkmn_red
A docker-based emulator for pokemon red, replace the dummy-rom file with a legitimate, legal ROM before running. Be sure to use a classic GB ROM since PyBoy doesn't support GBC or GBA by default.
